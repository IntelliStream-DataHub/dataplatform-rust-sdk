# What Read the Docs installs to build the Python reference.
# Pinned so a release of any of these cannot change the published site on its own.
sphinx==9.1.0
sphinx-autoapi==3.8.1
sphinx-rtd-theme==3.1.0
