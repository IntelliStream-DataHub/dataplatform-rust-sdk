# Read the Docs builds the Python client reference from the type stub; see docs-python/conf.py.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs-python/conf.py
  fail_on_warning: true

python:
  install:
    - requirements: docs-python/requirements.txt
